# ── 缓存策略（治理微信 WebView 顽固缓存）──────────────────────────
# Cloudflare Pages 规则：多条命中同一文件时，同名 header 会被「逗号拼接」，
# 因此这里全部写成【互不重叠】的按扩展名规则，绝不用 /* 兜底 Cache-Control。

# HTML 入口：永不硬缓存，每次都向服务器校验。
# 这是「改了代码微信看不到」的根治开关——HTML 一旦每次校验，就能立刻拿到
# 最新那个带 hash 的 JS/CSS 引用，所有代码/玩法更新随之生效。
/
  Cache-Control: no-cache
/index.html
  Cache-Control: no-cache

# Vite 构建产物（文件名带内容 hash，内容变则文件名变）：可安全永久缓存。
/assets/*.js
  Cache-Control: public, max-age=31536000, immutable
/assets/*.css
  Cache-Control: public, max-age=31536000, immutable

# 自托管字体（内容稳定）：永久缓存。
/fonts/*
  Cache-Control: public, max-age=31536000, immutable

# 游戏美术 PNG（固定文件名、无 hash）：存储但每次校验（走 ETag）。
# 换图后能立即刷新；未改动则服务器返回 304（无 body）几乎零开销。
/assets/*.png
  Cache-Control: public, max-age=0, must-revalidate
